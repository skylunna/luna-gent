[package]
name = "lunagent-core"
version = "0.1.0"
edition = "2021"
description = "Rust core for Luna-Gent"

[lib]
name = "lunagent_core"
crate-type = ["staticlib", "cdylib"]  # 可被Go调用

[dependencies]